{
  "name": "unifyr-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm install --prefix server && npm start --prefix server"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
